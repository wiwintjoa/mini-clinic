import{sql}from'drizzle-orm';import{boolean,check,date,index,integer,numeric,pgEnum,pgTable,primaryKey,text,timestamp,uniqueIndex,uuid}from'drizzle-orm/pg-core';
export const prescriptionStatus=pgEnum('prescription_status',['DRAFT','SUBMITTED','PROCESSING','READY','DISPENSED','CANCELLED']);
export const medicineCategories=pgTable('medicine_categories',{id:uuid('id').primaryKey().defaultRandom(),name:text('name').notNull().unique(),description:text('description'),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow()});
export const medicines=pgTable('medicines',{id:uuid('id').primaryKey().defaultRandom(),categoryId:uuid('category_id'),code:text('code').notNull().unique(),name:text('name').notNull(),genericName:text('generic_name').notNull(),unit:text('unit').notNull(),purchasePrice:numeric('purchase_price',{precision:14,scale:2}).notNull().default('0'),sellingPrice:numeric('selling_price',{precision:14,scale:2}).notNull().default('0'),minimumStock:integer('minimum_stock').notNull().default(0),isActive:boolean('is_active').notNull().default(true),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp('updated_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[index('medicines_name_idx').on(table.name),index('medicines_generic_idx').on(table.genericName),check('medicines_prices_check',sql`${table.purchasePrice}>=0 AND ${table.sellingPrice}>=0`)]);
export const prescriptionCounters=pgTable('prescription_counters',{counterDate:date('counter_date',{mode:'string'}).primaryKey(),nextValue:integer('next_value').notNull().default(1)},(table)=>[check('prescription_counter_positive_check',sql`${table.nextValue}>0`)]);
export const prescriptions=pgTable('prescriptions',{id:uuid('id').primaryKey().defaultRandom(),prescriptionNumber:text('prescription_number').notNull().unique(),patientId:uuid('patient_id').notNull(),doctorId:uuid('doctor_id').notNull(),visitId:uuid('visit_id').notNull().unique(),status:prescriptionStatus('status').notNull().default('DRAFT'),submittedAt:timestamp('submitted_at',{withTimezone:true}),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp('updated_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[index('prescriptions_status_idx').on(table.status),index('prescriptions_patient_idx').on(table.patientId)]);
export const prescriptionItems=pgTable('prescription_items',{id:uuid('id').primaryKey().defaultRandom(),prescriptionId:uuid('prescription_id').notNull(),medicineId:uuid('medicine_id').notNull(),dosage:text('dosage').notNull(),frequency:text('frequency').notNull(),route:text('route').notNull(),durationDays:integer('duration_days').notNull(),quantity:integer('quantity').notNull(),instructions:text('instructions'),dispensedQuantity:integer('dispensed_quantity').notNull().default(0),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp('updated_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[uniqueIndex('prescription_items_medicine_uidx').on(table.prescriptionId,table.medicineId),index('prescription_items_prescription_idx').on(table.prescriptionId),check('prescription_items_quantity_check',sql`${table.quantity}>0 AND ${table.durationDays}>0 AND ${table.dispensedQuantity}>=0`)]);
