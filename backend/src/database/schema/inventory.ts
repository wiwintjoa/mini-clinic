import{sql}from'drizzle-orm';import{check,date,index,integer,numeric,pgEnum,pgTable,text,timestamp,uniqueIndex,uuid}from'drizzle-orm/pg-core';export const stockTransactionType=pgEnum('stock_transaction_type',['PURCHASE','DISPENSE','ADJUSTMENT','RETURN','EXPIRED']);export const medicineBatches=pgTable('medicine_batches',{id:uuid('id').primaryKey().defaultRandom(),medicineId:uuid('medicine_id').notNull(),batchNumber:text('batch_number').notNull(),expiryDate:date('expiry_date',{mode:'string'}).notNull(),quantity:integer('quantity').notNull().default(0),purchasePrice:numeric('purchase_price',{precision:14,scale:2}).notNull(),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow(),updatedAt:timestamp('updated_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[uniqueIndex('medicine_batches_number_uidx').on(table.medicineId,table.batchNumber),index('medicine_batches_fefo_idx').on(table.medicineId,table.expiryDate),check('medicine_batches_quantity_check',sql`${table.quantity}>=0`)]);export const stockTransactions=pgTable('stock_transactions',{id:uuid('id').primaryKey().defaultRandom(),medicineId:uuid('medicine_id').notNull(),batchId:uuid('batch_id').notNull(),type:stockTransactionType('type').notNull(),quantity:integer('quantity').notNull(),reference:text('reference'),createdBy:uuid('created_by').notNull(),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[index('stock_transactions_medicine_idx').on(table.medicineId),index('stock_transactions_batch_idx').on(table.batchId),index('stock_transactions_created_idx').on(table.createdAt),check('stock_transactions_nonzero_check',sql`${table.quantity}<>0`)]);export const dispenseAllocations=pgTable('dispense_allocations',{id:uuid('id').primaryKey().defaultRandom(),prescriptionItemId:uuid('prescription_item_id').notNull(),batchId:uuid('batch_id').notNull(),quantity:integer('quantity').notNull(),createdAt:timestamp('created_at',{withTimezone:true}).notNull().defaultNow()},(table)=>[uniqueIndex('dispense_allocations_item_batch_uidx').on(table.prescriptionItemId,table.batchId),check('dispense_allocations_quantity_check',sql`${table.quantity}>0`)]);
