import{useQuery}from'@tanstack/react-query';import{api}from'../../app/api/client';export type InventoryMedicine={id:string;code:string;name:string;genericName:string;unit:string;minimumStock:number;totalStock:string|null};const unwrap=<T>(r:{data:{data:T}})=>r.data.data;export const useInventory=()=>useQuery({queryKey:['inventory'],queryFn:async()=>unwrap<InventoryMedicine[]>(await api.get('/inventory')),refetchInterval:30_000});
