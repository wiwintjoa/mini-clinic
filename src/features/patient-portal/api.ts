import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { api } from '../../app/api/client';
const unwrap = <T>(response: { data: { data: T } }) => response.data.data;
export type PortalProfile = { id: string; mrn: string; fullName: string; dateOfBirth: string; gender: string; phone: string; email: string | null; address: string; bloodType: string | null; allergies: string | null };
export type PortalAppointment = { id: string; appointmentDate: string; startTime: string; endTime: string; status: string; doctorName: string; specialty: string; serviceName: string };
export type PortalQueue = { id: string; queueNumber: string; status: string; checkedInAt: string; doctorName: string } | null;
export type PortalInvoice = { id: string; invoiceNumber: string; invoiceDate: string; grandTotal: string; amountPaid: string; status: string };
export type PortalDashboard = { profile: PortalProfile; upcomingAppointment: PortalAppointment | null; currentQueue: PortalQueue; recentVisits: Array<{ id: string; startedAt: string; chiefComplaint: string | null }>; recentPrescriptions: Array<{ id: string; prescriptionNumber: string; status: string; createdAt: string }>; outstandingInvoices: PortalInvoice[] };
export const usePortalDashboard = () => useQuery({ queryKey: ['portal', 'dashboard'], queryFn: async () => unwrap<PortalDashboard>(await api.get('/patient-portal/dashboard')) });
export const usePortalProfile = () => useQuery({ queryKey: ['portal', 'profile'], queryFn: async () => unwrap<PortalProfile>(await api.get('/patient-portal/profile')) });
export const usePortalAppointments = () => useQuery({ queryKey: ['portal', 'appointments'], queryFn: async () => unwrap<PortalAppointment[]>(await api.get('/patient-portal/appointments')) });
export const usePortalReferences = () => useQuery({ queryKey: ['portal', 'references'], queryFn: async () => unwrap<{ doctors: Array<{ id: string; fullName: string; specialty: string }>; services: Array<{ id: string; name: string; durationMinutes: number; price: string }> }>(await api.get('/patient-portal/references')) });
export const useBookAppointment = () => { const client = useQueryClient(); return useMutation({ mutationFn: async (input: { doctorId: string; serviceId: string; appointmentDate: string; startTime: string; endTime: string; notes?: string }) => unwrap(await api.post('/patient-portal/appointments', input)), onSuccess: () => client.invalidateQueries({ queryKey: ['portal'] }) }); };
export const useCancelAppointment = () => { const client = useQueryClient(); return useMutation({ mutationFn: async (id: string) => unwrap(await api.post(`/patient-portal/appointments/${id}/cancel`)), onSuccess: () => client.invalidateQueries({ queryKey: ['portal'] }) }); };
export const usePortalQueue = () => useQuery({ queryKey: ['portal', 'queue'], queryFn: async () => unwrap<PortalQueue>(await api.get('/patient-portal/queue')), refetchInterval: 15_000 });
export const usePortalHistory = () => useQuery({ queryKey: ['portal', 'history'], queryFn: async () => unwrap<Array<{ id: string; startedAt: string; completedAt: string | null; status: string; chiefComplaint: string | null; treatmentPlan: string | null; doctorName: string; diagnoses: Array<{ code: string; name: string; isPrimary: boolean }> }>>(await api.get('/patient-portal/history')) });
export const usePortalPrescriptions = () => useQuery({ queryKey: ['portal', 'prescriptions'], queryFn: async () => unwrap<Array<{ id: string; prescriptionNumber: string; status: string; createdAt: string; doctorName: string; items: Array<{ medicineName: string; dosage: string; frequency: string; route: string; durationDays: number; quantity: number; instructions: string | null }> }>>(await api.get('/patient-portal/prescriptions')) });
export const usePortalInvoices = () => useQuery({ queryKey: ['portal', 'invoices'], queryFn: async () => unwrap<PortalInvoice[]>(await api.get('/patient-portal/invoices')) });
export const usePortalInvoice = (id: string | null) => useQuery({ queryKey: ['portal', 'invoice', id], queryFn: async () => unwrap<PortalInvoice & { subtotal: string; discount: string; tax: string; balance: string; items: Array<{ id: string; description: string; quantity: number; unitPrice: string; total: string }>; payments: Array<{ id: string; paymentNumber: string; amount: string; method: string; paidAt: string }> }>(await api.get(`/patient-portal/invoices/${id}`)), enabled: Boolean(id) });
