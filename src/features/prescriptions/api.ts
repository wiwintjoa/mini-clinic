import{useMutation,useQuery,useQueryClient}from'@tanstack/react-query';import{api}from'../../app/api/client';export type Medicine={id:string;code:string;name:string;genericName:string;unit:string};export type PrescriptionItem={id:string;medicineId:string;medicineCode:string;medicineName:string;genericName:string;unit:string;dosage:string;frequency:string;route:string;durationDays:number;quantity:number;instructions:string|null;dispensedQuantity:number};export type Prescription={id:string;prescriptionNumber:string;status:string;visitId:string;patientName:string;mrn:string;doctorName:string;items:PrescriptionItem[]};const unwrap=<T>(response:{data:{data:T}})=>response.data.data;export const usePrescription=(visitId:string)=>useQuery({queryKey:['prescription','visit',visitId],queryFn:async()=>unwrap<Prescription|null>(await api.get(`/prescriptions/visit/${visitId}`))});export const useMedicines=()=>useQuery({queryKey:['medicines','prescribing'],queryFn:async()=>unwrap<Medicine[]>(await api.get('/pharmacy/medicines')),staleTime:60_000});const usePrescriptionMutation=(action:(input:{id:string;data?:unknown})=>Promise<unknown>)=>{const client=useQueryClient();return useMutation({mutationFn:action,onSuccess:()=>client.invalidateQueries({queryKey:['prescription']})});};export const useCreatePrescription=()=>usePrescriptionMutation(async({id:visitId})=>unwrap(await api.post('/prescriptions',{visitId})));export const useAddPrescriptionItem=()=>usePrescriptionMutation(async({id,data})=>unwrap(await api.post(`/prescriptions/${id}/items`,data)));export const useSubmitPrescription=()=>usePrescriptionMutation(async({id})=>unwrap(await api.post(`/prescriptions/${id}/submit`)));
